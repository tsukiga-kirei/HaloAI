# HaloAI 仓库开发约束

## 产品目标

HaloAI 是一个以团队为核心、由人主导的协作空间。团队成员与身份明确的 AI 成员在房间、任务和文档中并肩工作，把讨论推进为可交付成果。AI 不得隐藏责任归属，也不得静默替代人的权限与最终决定。

## 工作顺序

1. 修改重要功能前，先检查 `docs/` 中对应的中英文规格和安全不变量。
2. 规格缺失或会改变产品边界时，先补文档和验收条件，再实现代码。
3. 优先打通一条可验证的端到端链路，不提前堆叠没有用户路径的页面或基础设施。
4. 任何架构取舍都必须说明为什么适合当前阶段，以及何时需要升级。

## 语言与注释

- 应用、领域、API、Worker 和 Agent 编排逻辑统一使用 TypeScript。
- CSS、SQL migration、JSON、Markdown 和容器配置属于允许的支撑格式。
- 代码模块说明、领域不变量、状态机、安全边界、并发处理和非显然取舍必须使用完整中文注释。
- 不为显而易见的语法逐行注释；注释应解释“为什么”和“不可破坏的约束”。
- 所有用户可见文案必须进入类型化国际化目录，不允许散落硬编码。
- `zh-CN` 与 `en-US` 必须保持键和值域一致，并通过自动化检查。
- `docs/en-US/` 与 `docs/zh-CN/` 必须使用相同文件名并保持语义一致；修改时同步维护。

## 领域和安全不变量

- `Actor`、访问角色 `AccessRole`、AI 人设 `Persona/AgentProfile` 必须分离。
- AI 不得获得或模拟人类登录会话；每次运行都必须记录委托人、Agent 版本与授权快照。
- 权限必须由服务端策略强制执行，不能依赖提示词、按钮隐藏或客户端传入的租户 ID。
- 租户和项目上下文缺失时必须拒绝访问，禁止 fail-open。
- 模型输出、上传文件、网页、检索结果和 MCP 响应统一视为不可信数据。
- 检索必须先做权限过滤，再进入排序和模型上下文。
- 外部写入、公开发布、删除、付款、权限修改和敏感数据操作默认要求人工审批。
- AI 不得创建权限、扩大工具范围、审批自己的行为或自动安装插件/MCP。
- 每次 Agent 运行必须有 token、费用、时长、轮次、工具调用和参与者上限，并支持取消。
- 高风险批准必须绑定具体工具、参数摘要、Agent、委托人和有效期。

## 架构边界

- `apps/web`：交互界面和当前阶段的 BFF 路由，不承载长期后台任务。
- `apps/api`：承载认证边界、REST、SSE、策略入口和实时协作握手。
- `apps/collab`：承载 Yjs/Hocuspocus 文档同步；只接受服务端签发的短期票据，不拥有授权真相。
- `apps/worker`：承载可恢复的 Agent、Outbox 和投影任务。
- `packages/core`：不依赖 UI 和模型 SDK 的领域类型与规则。
- `packages/contracts`：跨网络、队列和进程边界的运行时校验，不负责做授权决定。
- `packages/agent-runtime`：耐久状态机与显式协作路由，不拥有产品身份和权限真相。
- `packages/model-gateway`：隔离具体模型 SDK，输出 HaloAI 自己的流式事件协议。
- `packages/db`：租户感知的 schema、RLS 和 repository 契约。
- `docs`：产品、架构、威胁模型、决策和运行规范。
- `tests/e2e`：桌面、移动、语言、主题和关键端到端协作路径的浏览器验收。

依赖方向必须从应用指向领域包；领域包不得反向依赖 Next.js、数据库或具体模型 SDK。

## 前端质量

- 桌面采用房间、对话、文档三栏结构；移动端采用单视图栈和底部导航，不得简单压缩桌面布局。
- 所有关键操作必须有键盘、触摸和屏幕阅读器可用路径。
- 使用 `100dvh`、safe-area、可缩放 viewport，并处理软键盘与输入草稿。
- 尊重 `prefers-reduced-motion`；交互目标不小于 44×44 CSS 像素。
- 不允许巨型页面文件。单文件接近 500 行时应优先按领域和视图职责拆分。
- 设计系统必须使用语义 token，支持明暗主题，不能在页面中大量散落颜色常量。

## 验证

- 改变策略、路由、验证、Agent 状态机或并发行为时必须同步测试。
- 交付重要修改前运行 `pnpm check`。
- 页面修改至少检查 390×844、768×1024、1440×900 三种视口，以及中英文和明暗主题。
- 若外部服务阻止某项检查，应说明缺失依赖，并完成其余所有可运行检查。
